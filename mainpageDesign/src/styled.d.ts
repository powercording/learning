import { WhileStatement } from "typescript";

declare module "styled-components" {
  export interface DefaultTheme {
    backColor: string;
    themeTextColor: string;
    themeFocusedColor: string;
    accentColor: string;
  }
}
